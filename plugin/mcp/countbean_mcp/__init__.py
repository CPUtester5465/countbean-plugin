"""Countbean MCP server package."""
# The distribution version. pyproject.toml reads it from here
# (`tool.setuptools.dynamic`), and test_plugin_packaging.py binds it to
# plugin/.claude-plugin/plugin.json so the marketplace and PyPI cannot
# advertise different versions of the same code.
__version__ = "0.5.3"
